[user]
	name = Matthew Gerstemeier
	email = mgerstemeier@cas.org
[credential]
	helper = !aws codecommit credential-helper $@ --profile default
	UseHttpPath = true
[diff]
	tool = nvimdiff
	renames = true
[difftool]
	prompt = false
[difftool "nvimdiff"]
	cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""
[user]
	name = Matthew Gerstemeier
	email = mgerstemeier@cas.org
[alias]
	s = status -sb
	#lg = log --pretty=format:\"%h%x09%an%x09%ad%x09%s\"
	ll = log --color --graph --date=format:'%D - %H:%M:%S' --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ad) %C(bold blue)<%an>%Creset'
	lls = log --color --graph --date=format:'%D - %H:%M:%S' --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ad) %C(bold blue)<%an>%Creset'
	sh = show --pretty="" --name-only
    c = commit -a
    r = reset --hard HEAD
    d = difftool
    db = branch -D
    cb = checkout -b
    sb = checkout
    ps = push
    pl = pull
    f = fetch
	
[core]
#	editor = code --wait
[commit]
	template = ~/.gitmessage
[core]
	editor = vim
	excludesfile = /home/mpg26/.cas-git-tools/etc/default-excludesfile
[init]
	defaultBranch = master 
[merge]
	tool = kdiff3
[mergetool "kdiff3"]
	keepBackup = false
	trustExitCode = false
[color]
	ui = true
[gerrit]
	createchangeid = true
[casgittools]
	gerritAlias = osd
	sshAlias = gerrit
	httpServer = osd-review.cas.org
